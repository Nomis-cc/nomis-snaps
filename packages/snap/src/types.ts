/* eslint-disable @typescript-eslint/naming-convention */
export type Holder = {
  isHolder: boolean;
  score: number;
  scoreName: string;
  url: string;
  image: string;
  tokenId: string;
  updated_ms: string;
  referralCode: string;
};

export type Score = {
  score: number;
  scoreName: string;
  url: string;
};
